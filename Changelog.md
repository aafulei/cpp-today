# Changelog

## 0.1.0

*2025-05-12*

### Added

- Initial release of the `today` CLI program that prints the current date in
  `YY/MM/DD = Www` format.
- Simple `make` build system.
- Integrated `clang-format` configuration for code formatting.
- Editor tooling support via `clangd`.
